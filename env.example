# Database
DATABASE_URL="file:./dev.db"

# Solana RPC Endpoint
# IMPORTANT: The public endpoint has strict rate limits and will cause 429 errors
# For production use, get a dedicated RPC endpoint from:
# - Helius: https://helius.xyz/ (recommended)
# - QuickNode: https://quicknode.com/
# - Alchemy: https://alchemy.com/
# - Or run your own Solana validator

# Public endpoint (rate limited - for testing only):
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT="https://api.mainnet-beta.solana.com"

# Example with Helius (replace YOUR_API_KEY with actual key):
# NEXT_PUBLIC_SOLANA_RPC_ENDPOINT="https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY"

# Optional: RPC API Key (if using a provider that requires authentication)
SOLANA_RPC_API_KEY=""

# Optional: Helius API Key for enhanced data fetching
HELIUS_API_KEY=""

# Application Settings
NEXT_PUBLIC_APP_NAME="Solana Wallet Tracker"
NEXT_PUBLIC_DEFAULT_REFRESH_INTERVAL="30000" # 30 seconds
